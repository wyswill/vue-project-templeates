syntax = "proto3";
package hero;


service hero_service {
  rpc Test (test) returns (Hero) {}
}

message HeroById {
  int32 id = 1;
  message Userinfo{
    string name=1;
    int32 age=2;
  }
}

message test{
  HeroById.Userinfo info=1;
}

message Hero {
  int32 id = 1;
  string name = 2;
}
